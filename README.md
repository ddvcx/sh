
apt -y install curl

bash <(curl -sL "http://github.com/ddvcx/sh/raw/m/c.sh?t=$RANDOM")

bash <(wget –no-check-certificate -qO- http://github.com/ddvcx/sh/raw/m/c.sh)

bash <(wget –no-check-certificate -qO- http://github.com/ddvcx/sh/raw/m/dd.sh) -d 11 -v 64 -a -p 密码 #Debian 11

bash <(wget –no-check-certificate -qO- http://github.com/ddvcx/sh/raw/m/dd.sh) -u 20.04 -v 64 -a -p 密码 #ubuntu 20，默认密码 MoeClub.org
